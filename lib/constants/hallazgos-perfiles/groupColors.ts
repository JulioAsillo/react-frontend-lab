/**
 * groupColors.ts — Certificación de Perfiles.
 *
 * Vacío a propósito: tú vas a decidir qué columnas agrupar y de qué color
 * (dijiste que ya sabes el criterio). Acá solo va la traducción
 * "nombre de grupo" → clase de color (th-src-1 .. th-src-15, ver
 * app/globals.css y lib/constants/headerPalette.js).
 *
 * Ejemplo de cómo llenarlo una vez que decidas los grupos:
 *   export const GROUP_COLORS: Record<string, string> = {
 *     EXACTUS: "th-src-6",
 *     SDP:     "th-src-12",
 *     SIT:     "th-src-3",
 *     NPAC:    "th-src-14",
 *     GDH:     "th-src-5",
 *     MR:      "th-src-11",   // Matriz de Roles
 *     VALIDACION: "th-src-2",
 *   };
 *
 * Los grupos que uses acá deben coincidir exactamente con el campo `group`
 * que pongas en cada hallazgo-*.ts de esta carpeta.
 */
export const GROUP_COLORS: Record<string, string> = {
  // completar
};
