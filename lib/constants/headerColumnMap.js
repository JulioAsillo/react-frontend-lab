/**
 * headerColumnMap.js — DEPRECADO.
 *
 * Reemplazado por lib/constants/columnDefs/ (ver index.ts), que además de
 * color trae el header legible por columna, extraído directamente de los
 * .py de backend. Nada en el proyecto importa este archivo; se deja vacío
 * a propósito para no confundir con un import viejo.
 */
export const HEADER_COLUMN_MAP = {};
