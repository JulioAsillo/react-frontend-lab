import LoginScreen from "@/components/auth/LoginScreen";

export const metadata = { title: "Ingresar — Certificación de Usuarios" };

export default function LoginPage() {
  return <LoginScreen />;
}
